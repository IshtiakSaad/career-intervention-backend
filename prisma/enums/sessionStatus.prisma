enum SessionStatus {
    PENDING
    CONFIRMED
    ONGOING
    COMPLETED
    SETTLED             // Dispute window closed, funds released
    CANCELLED_BY_MENTEE
    CANCELLED_BY_MENTOR
    EXPIRED             // Mentor didn't respond within SLA
    NO_SHOW
    DISPUTED
    REFUNDED
    REJECTED
}
